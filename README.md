# Eis — Personal Portfolio

A personal portfolio website built with Vite + React. Originally hosted on GitHub Pages, now running in a Docker dev environment via Base44.

## Development

```bash
docker compose -f docker-compose.base44.yml up -d
```

App runs at http://localhost:3000 with hot reload.
